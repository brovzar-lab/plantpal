rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Users may only upload/download plant images to their own prefix
    match /plants/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
